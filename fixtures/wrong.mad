-- import Something from "Somewhere"

where("str") {
  is Just a: "ok"
}


3 + 2 :: String

fn :: Num -> Num
fn = (x) => "OK"

-- if ("False") {
--   "ok"
-- } else {
--   3
-- }

-- inc = (a) => a + b

-- fn = (a, b, c) => a === c

-- fn(2, "3", "3")