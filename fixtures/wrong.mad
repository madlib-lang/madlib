-- import Something from "Somewhere"

where("str") {
  is Just a: "ok"
}


3 + 2 :: String

fn :: Number -> Number
fn = (x) => "OK"

-- if ("false") {
--   "ok"
-- } else {
--   3
-- }

-- inc = (a) => a + b

-- fn = (a, b, c) => a === c

-- fn(2, "3", "3")
