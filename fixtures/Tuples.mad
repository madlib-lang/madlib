
x = <1, 2, 3 > 4>


<1, 2, 3> == <1, 2, 3>

where(<1, 2>) {
  is <a, b>: a + b
}

fn :: Number -> <Number, Number>
fn = (a) => (<a, a>)


fst :: <a, b> -> a
fst = (tuple) => (where(tuple)
  is <a, _>: a
)

snd :: <a, b> -> b
snd = (tuple) => (where(tuple)
  is <_, b>: b
)

fst(<1, 2>)
snd(<1, 2>)
