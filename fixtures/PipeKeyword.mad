import IO from 'IO'

pipe(
  (z) => (z),
  (z) => (z * 2),
  (z) => (z + 2),
  (z) => (z + 2),
  IO.log
)(3)

fn = pipe(
  (x) => (x + 1),
  (x) => (x * 2),
  where
    is 8: 16
    is _: 2
  ,
  where
    is 16: 32
    is _ : 2
)

fn(3) |> IO.log




data Maybe a = Just a | Nothing

fn2 = where is Just x: x

fn2(Just(3)) |> IO.log

