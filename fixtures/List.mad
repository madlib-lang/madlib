
singleton :: a -> List a
export singleton = (a) => [a]

map :: (a -> b) -> List a -> List b
export map = (f, xs) => #- xs.map(f) -#

-- -- map2 :: (a -> b) -> List a -> List b
-- export map2 = (f, xs) => #- xs.map(f) -#
