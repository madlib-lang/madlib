import L  from "List"
import IO from "IO"
import F  from "Future"

id = (a) => a
double = (n) => n * 2

F.of([3, 4])
  |> F.map(L.map(double))
  |> F.run(IO.print, IO.print)

-- get("http://www.google.com")
--   |> run(id, print)

L.singleton(3) |> IO.print

t = (x) => if (x) { False } else { True }

F.of(True)
  |> F.map((x) => if (x) { 2 } else { 4 })
  |> F.map((x) => x * 2)
  |> F.run((x) => x, IO.print)
