The 3rd list element has a different type
the 3rd element is
  String
but the elements before it are
  Float
Note: All elements of a list must share one type. For mixed data, define a union type: type Item = A(...) | B(...).