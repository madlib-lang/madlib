Cannot mutate function
'myFn' is a function and cannot be mutated with ':='.
Note: Functions are immutable values. You cannot reassign them.
Hint: To make 'myFn' swappable, wrap it in a mutable record:
  ref = { fn: myFn }
  ref.fn := newFunction    // mutate the record field