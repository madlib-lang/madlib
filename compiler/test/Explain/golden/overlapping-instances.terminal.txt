[error]: Two instances overlap for 'Show'
     ╭──▶ test/Module.mad@1:1-1:20
     │
   1 │ someValue = doStuff(1, 2)
     • ┬────────────────── 
     • ╰╸ 'Show String' overlaps with the existing instance 'Show a'.
     •
     │ Hint: Remove one of the two instances, or narrow their types so only one can ever apply to a given value.
─────╯
