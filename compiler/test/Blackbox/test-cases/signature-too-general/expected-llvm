     ╭──▶ compiler/test/Blackbox/test-cases/signature-too-general/Entrypoint.mad@4:1-5:26
     │
   4 │ ╭┤ f :: String -> a -> String
   5 │ ├┤ f = (s, x) => s ++ x.name
     • │ 
     • ╰╸ Type signature given:
     •        String -> a -> String
     •    
     •    Type inferred:
     •        String -> { ...base, name :: String } -> String
     •    
     •
     │ Note: The annotation claims the function is more polymorphic than the implementation allows.
     │       The inferred type is more specific — it uses concrete types or fewer type variables.
     │ Hint: Update the type annotation to match the inferred type shown above.
─────╯

