echo "Installing madlib from local package, if the version specified in"
echo "pkg/package.json is not a published release it'll fail, you would then"
echo "need to edit it first and then you can use the 'update-pkg-build' script"
echo "in order to get your locally built madlib globally installed on your system"

cd pkg;
npm install;
npm link;
