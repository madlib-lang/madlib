#!/bin/bash

# Input:
# CXX: c++ compiler to use, default g++
# CXXFLAGS: extra c++ flags to use, default none
# AR: archiver to use, default ar
# TARGET: one of WIN_X64, LINUX_X64, MUSL_X64, MACOS_X64, MACOS_ARM64, default MACOS_ARM64

make -C runtime -j
