import {} from "./PrettyShow"

log :: String -> ()
log = extern "puts"

export log


trace :: String -> a -> a
export trace = (s, a) => {
  log(s)
  return a
}


prettyLog :: PrettyShow a => a -> ()
export prettyLog = pipe(
  ppShow,
  log
)
